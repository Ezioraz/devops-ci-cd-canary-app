{
  "name": "ci-cd-canary-app",
  "version": "1.0.0",
  "description": "Sample Node.js API for CI/CD + Canary Deployment project",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.1.1"
  }
}
